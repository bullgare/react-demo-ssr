import React from 'react';

const HomePage = () => {
    return (
        <div>The home component!12<button onClick={() => console.log('!1')}>click</button></div>
    );
};

export default {
    component: HomePage
};